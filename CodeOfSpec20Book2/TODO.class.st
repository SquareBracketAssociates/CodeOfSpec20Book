"
- we should revisit the miniClassBrowser (remove the api-event stuff) and see how we can browse the comments using probably the mic renderer.

- we should write a test that execute all the class side example methods. 
"
Class {
	#name : #TODO,
	#superclass : #Object,
	#category : #'CodeOfSpec20Book2-Windowing'
}
